# Upstream Sync Reports

Create one report per integrated upstream release or synchronization point. Include old and new base commits, conflicts by VShoon patch ID, changed dependencies, automated results, manual checks, and rollback notes.
