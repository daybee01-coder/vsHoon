{
	"displayName": "VShoon Start Window",
	"description": "Contributes the settings that govern the VShoon start window.",
	"configuration.title": "VShoon",
	"configuration.enabled.description": "Show the VShoon start window when VShoon is launched without a file, folder, or workspace to open. When disabled, such a launch opens the workbench directly. Use `--disable-start-window` to skip the start window for a single launch."
}
